// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}
// DATABASE_URL="file:./db.sqlite"
datasource db {
  provider = "postgresql"
  // provider = "sqlite"
  url   = env("DATABASE_URL")
  relationMode = "prisma"
}

model Post {
    id        Int      @id @default(autoincrement())
    name      String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@index([name])
}

//user linked to clerk login
model User {
    id        String      @id @default(cuid())
    name      String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    //should have a list of labs they have access to
    labs Lab[]
    organizations OrganizationUser[]
}

//lab state: how many labs are completed? What is the status of the labs?
model Lab {
  id String @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  name String
  // completed, active
  status String
  progress Int @default(0)

  user User @relation(fields: [userId], references: [id])
  userId String

  @@index([userId])
}


model Organization {
  id        String   @id @default(cuid())
  name      String
  // changed to string array instead of labs because labs are individual
  labs      String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
  // organization should have a list of users
  users     OrganizationUser[]
}

model OrganizationUser {
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  organizationId String
  organization  Organization @relation(fields: [organizationId], references: [id])

  @@id([userId, organizationId])
  @@index([organizationId])
}